# Progressive repository maps

These maps are an index for agents and contributors, not a duplicate of architecture documentation. Load the map matching the task after `AGENTS.md`, then follow only its links.

When adding a durable subsystem, add or update a map so a new contributor can find the right contracts without reading unrelated source code.
