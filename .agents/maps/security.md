# Security map

Read `docs/security-model.md` before changing ingestion, retrieval, sharing, skills, policies, or protocol adapters. Treat memory and retrieved documents as untrusted input. Enforce authorization and policy outside the LLM.
