# Web map

The future `packages/hangar-admin` console uses Next.js, TypeScript, and shadcn/ui. Its first screens are in `docs/admin-console.md`. It is an administration client of the core API; it must not access stores directly or become an authorization authority.
